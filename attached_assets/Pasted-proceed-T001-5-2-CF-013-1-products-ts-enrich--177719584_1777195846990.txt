proceed.

T001.5 라운드 2 승인. CF-013 자가 수정은 검증 게이트가 신뢰할 수 
있다는 두 번째 증거 (라운드 1: products.ts enrich(), 라운드 2: 
timestamp count). 신뢰도 ↑↑.

15개 CF + MONEY_AUDIT §5 모두 적정. P0=3, P1=10, P2=2 분포가 PMS 
실제 위험에 부합.

═══════════════════════════════════════════════════
[T002 시작 — 추가 안전장치]
═══════════════════════════════════════════════════

T002는 산출물이 12+ 파일로 가장 큰 단계입니다. 시작 전 한 가지 
선결 조건만 추가합니다.

[T002.0 — Pre-flight, 30분 이내]

산출물: `_schema/_T002_PLAN.md` (작업 계획서)

내용:
1. 작업 순서 명시
   순서: api-endpoints/INDEX.md → 8개 도메인 파일 → 
   db-schema-overview.md → erd-core.md → state-machines.md
   
   이유 명시: 왜 이 순서인가?
   (예: INDEX 먼저 = 도메인 분류 확정 후 deep dive 안정적)

2. 도메인 분류 사전 합의
   353 endpoints → 8 도메인 매핑.
   각 도메인의 정의와 경계 사례 제시.
   
   특히 경계가 애매한 케이스 5개 이상:
   - /api/v1/contracts/.../invoices/* → contract.md? finance.md?
   - /api/v1/admin/bookings/* → booking.md? admin.md?
   - 이런 경계 케이스를 사전에 결정하지 않으면 T002 중간에 
     reshuffling 발생.
   
   결정 원칙 명시 (예: "primary entity 기준" or "URL prefix 기준")

3. INDEX.md 컬럼 스키마 확정
   초안: Domain | File | Endpoint Count | Auth Type | Public Y/N
   
   추가 검토:
   - "Side Effect Density" 컬럼 추가 여부 (ex: HIGH/MED/LOW)
   - "Audit Coverage %" 컬럼 추가 여부 (CF-008 추적)
   - "Money Touching Y/N" 컬럼 추가 여부 (재무 영향 endpoints 필터링)
   
   각 컬럼의 가치와 비용 trade-off 평가 후 최종 스키마 확정.

4. 도메인 파일 표준 포맷 sample
   booking.md의 첫 5개 endpoint를 실제로 작성해서 sample 제출.
   포맷이 적절한지 내가 확인 후 나머지 348개 진행.
   
   이게 핵심: 348개 작성한 후 포맷 잘못이면 재작업 막대함.
   5개 sample → 승인 → 나머지 일괄 진행.

5. ERD cluster 계획
   55+ 테이블을 한 다이어그램에 그리면 가독성 0.
   도메인별 cluster 분할 계획:
   - Property cluster (properties / spaces / amenities / images)
   - Booking cluster (bookings / quotes / availability)
   - Contract cluster (contracts / line_items / extensions)
   - Finance cluster (invoices / payments / payment_info)
   - Identity cluster (users / accounts / sessions)
   - CRM cluster (contacts / leads / tasks / commissions)
   - Ops cluster (work_orders / services / tickets)
   
   각 cluster 별 다이어그램 + 마지막에 cluster간 관계만 보여주는 
   상위 다이어그램 1개. 총 8개 Mermaid 다이어그램.

6. Dead tables 처리 방침
   CF-009의 dead tables (products / product_catalog) + 다른 dead 
   가능성 있는 테이블 사전 식별.
   
   ERD에 표시 방법:
   (a) 회색/취소선으로 dead 표시
   (b) ERD에서 제외하고 별도 "Dead Tables" 섹션
   (c) 정상 표시 + 노트만
   
   추천 방향과 이유 제시.

7. 검증 게이트 분할 계획
   T002 산출물별 검증 시점:
   - INDEX.md + 8 도메인 파일 → 1차 검증 (3 클레임)
   - db-schema-overview.md → 2차 검증 (3 클레임)
   - erd-core.md → 3차 검증 (3 클레임)
   - state-machines.md → 4차 검증 (3 클레임)
   
   = 총 4회 검증, 12 클레임 재확인.

8. 예상 산출물 크기
   각 파일 예상 line count.
   특히 도메인 파일 8개의 평균 endpoint 수와 line 수.
   상한선 설정 (예: 단일 파일 1500 lines 초과 시 분할).

═══════════════════════════════════════════════════
[T002.0 후속 절차]
═══════════════════════════════════════════════════

T002.0 완료 → 보고 → 내가 검토 → 
조정 사항 있으면 반영 → "T002 정식 시작" proceed → 
T002.1 (INDEX.md + 도메인 sample 5개) → 
내 sample 승인 → 
T002.2 (나머지 348개 endpoint 도메인 파일 완성) → 
검증 1차 → 
T002.3 (db-schema-overview) → 검증 2차 → 
T002.4 (erd-core) → 검증 3차 → 
T002.5 (state-machines) → 검증 4차 → 
T002 완료

각 단계 완료 시 명시적 proceed 대기.

═══════════════════════════════════════════════════
[지금 시작]
═══════════════════════════════════════════════════

T002.0 _schema/_T002_PLAN.md 작성 시작.
30분 이내 완료 목표. 완료 시 보고 → 내 검토 → 정식 T002 시작.