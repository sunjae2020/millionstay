T002.1 검토 완료. 포맷 (a) lock — 단, T002.2 시작 전 minor 보강 3건 
적용 후 진행.

═══════════════════════════════════════════════════
[포맷 평가]
═══════════════════════════════════════════════════

✅ S2 적중 — 163줄 핸들러에 5 finding anchor = worst case 검증 완료
✅ 5 카테고리 booking.md 내 자연 매칭 = 도메인 풍부도
✅ 35-cell self-check = 누락 즉시 식별
✅ S5 Zod 누락 자발 발견 = 패턴 인식 능동성

이 포맷으로 348개 진행 가능.

═══════════════════════════════════════════════════
[T002.1.5 — 포맷 lock 전 minor 보강]
═══════════════════════════════════════════════════

시작 전 다음 3건 적용:

[B1] Endpoint 메타 표준 헤더 1줄 추가
각 endpoint sample에 다음 메타가 한 곳에 모여 있어야 348개 grep 
용이:

`Auth: <guard> | $$: <Y/N> | logAction: <Y/N> | CF: <CF-### list>`

위치: endpoint 제목 바로 아래 1줄. 본문 분산이 아니라 **단일 줄 
헤더**.

이유: 
- 348개 작성 후 "audit 누락 endpoint 전부 찾아" 같은 grep 한 번에.
- Phase 2 마이그레이션 시 risk priority 정렬 즉시 가능.
- 현재 sample들은 정보가 본문 분산되어 grep 어려움.

[B2] booking.md sample 5개에 후행 검증 1회 추가
T002.2 진행 전, 5 sample을 다시 한 번 자가 검증:

각 sample에 대해:
- 인용된 file:line이 실제 코드와 일치하는지 spot-check (1개씩 재확인)
- "DB writes 테이블명"이 schema에 실제 존재하는지 grep 확인
- CF cross-ref가 CRITICAL_FINDINGS.md의 해당 ID와 일치하는지

오류 발견 시 즉시 수정. 검증 결과를 booking.md 말미에 
"Sample Re-Verification Log" 섹션으로 기록 (검증 일자 / 검증한 
클레임 / 결과).

이유: 5 sample이 348개의 ground truth가 되므로 검증 1회 더 필요. 
T002.0에서 PRODUCTS.ts:29 enrich 오인을 V3 검증에서 잡은 패턴 반복.

[B3] T002.2 작성 시 progressive verification 도입
348개를 한꺼번에 작성하지 말고 도메인별 분할:

T002.2.a: contract.md 작성 → 자체 spot-check 3 클레임 → 보고 → 
          내 proceed 후 다음
T002.2.b: finance.md 작성 → spot-check → 보고 → proceed
T002.2.c: ops-property.md 작성 → spot-check → 보고 → proceed
T002.2.d: ops-catalog.md 작성 → spot-check → 보고 → proceed
T002.2.e: ops-crm.md 작성 → spot-check → 보고 → proceed
T002.2.f: portal-guest.md 작성 → spot-check → 보고 → proceed
T002.2.g: portal-partner.md 작성 → spot-check → 보고 → proceed
T002.2.h: public.md 작성 → spot-check → 보고 → proceed
T002.2.i: admin.md 작성 → spot-check → 보고 → proceed

각 도메인 완료 후 _T002_PROGRESS.md 갱신 + 짧은 보고 (50줄 이내):
- 작성 endpoint 수 / 누적 endpoint 수 (348 진행률)
- logAction 커버리지 (도메인 내)
- $$ touching 수
- 발견된 추가 CF 후보 (있는 경우)
- spot-check 결과 (3 클레임 통과 여부)

이 프로토콜로 9 도메인 = 9 보고. 각 보고 후 내 proceed.

이유:
- 348개 일괄 작성 후 검증하면 환각 누적 발견 어려움.
- 도메인별 작성 시 점진적 패턴 일관성 강화.
- 중간에 발견되는 신규 CF (예: 새로운 dead table, 새로운 audit gap)
  를 즉시 CRITICAL_FINDINGS에 추가 가능.
- 컨텍스트 회복 비용 분산 (한 도메인 = 한 세션 단위).

═══════════════════════════════════════════════════
[리포지토리 일관성 운영 규칙 — 이후 모든 태스크 영구 적용]
═══════════════════════════════════════════════════

[R-REPO-1] 모든 산출물 변경 시 동반 갱신 필수
이후 어떤 문서를 작성/수정하든, 다음 4개 메타 파일은 **항상 함께** 
갱신:

(a) `.local/session_plan.md` — 태스크 상태 갱신
(b) `_T002_PROGRESS.md` (T002 진행 중) 또는 해당 단계 PROGRESS 파일
(c) 영향받는 마커 (REVISE → 새 파일 대체 시 ARCHIVE 마커)
(d) cross-ref 받는 다른 문서 (예: CF 추가 시 INDEX의 Risk 컬럼 
    재계산)

규칙: 한 산출물만 갱신하고 메타 파일을 미루지 않는다. 모든 변경은 
**원자적 묶음**으로 커밋.

[R-REPO-2] 커밋 메시지 표준
형식: `docs(reverse): <task-id> <action> <target>`

예:
- `docs(reverse): T002.1 add INDEX.md + booking.md sample (5)`
- `docs(reverse): T002.1.5 add metadata header to samples`
- `docs(reverse): T002.2.a add contract.md (38 endpoints)`

이유: Git history만으로 진행 상황 추적 가능.

[R-REPO-3] 매 태스크 시작 전 컨텍스트 anchor 재독
메모리 의존 절대 금지. 다음 파일을 항상 먼저 read:

(a) `.local/session_plan.md` — 전체 위치 파악
(b) 해당 단계 PROGRESS 파일 (예: `_T002_PROGRESS.md`)
(c) `_audit/CRITICAL_FINDINGS.md` — CF cross-ref용
(d) 직전 단계 산출물

Read 후 "Re-read context: <files>" 1줄 출력 후 작업 시작.

[R-REPO-4] 산출물 보고 형식 표준
모든 단계 완료 보고는 다음 구조: