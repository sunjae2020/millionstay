proceed — corrected 5 entities enumeration + 모두 ⓑ 채택 + CF-010 
본문 재작성.

R-REPO-9 차단 게이트 2회째 가동 (R-REPO-6 10번째) 성공. 영구 패턴 
confirm.

═══════════════════════════════════════════════════
[사용자 환각 9-10번째 종합 인정]
═══════════════════════════════════════════════════

저(사용자) 환각 5 entities 모두:
- contracts 4 → 7 state (3 누락: Sent/Signed/Archived)
- invoices 5/5 가짜 (Paid만 일치, Stripe 추정 의존)
- work_orders "free-transition" → 6 values
- bookings 9th = "Pending" outlier
- cs_tickets 3 OK ✅

원인: T002.3 db-schema-overview.md + T002.2.a/e 재독 안 함 
(R-REPO-3 어김)

R-REPO-6 10번째 가동 = 영구 가치 재확인.

═══════════════════════════════════════════════════
[Decision] Corrected 5 entities enumeration 채택
═══════════════════════════════════════════════════

1. bookings: 8 main + 1 "Pending" outlier (F7 incidental)
   Draft / PendingPayment / PendingApproval / Confirmed / Active / 
   CheckedOut / Cancelled / Archived + ⚠️ "Pending" (guest-portal:160)
   
2. contracts: 7-state
   Draft / Sent / Signed / Active / Terminated / Expired / Archived
   + contract_line_items.status: Deleted (별도 테이블, 본 entity 외)
   
3. invoices: 5-state + stripe_status 별도 컬럼
   invoices.status: Draft / Sent / Paid / Archived / Void
   stripe_status: payment_failed / refunded (audit-only, status 미변경)
   
4. work_orders: 6-state
   Open / InProgress / Completed / Cancelled / PendingReview / Archived
   "free-transition" 평가는 Step 2 precondition 가드 enumerate 후 확정
   
5. cs_tickets: 3-state
   Open / InProgress / Archived

═══════════════════════════════════════════════════
[Decision] 모두 ⓑ 채택
═══════════════════════════════════════════════════

(가) Status taxonomy ⓑ — ground truth + "사용자 안 가짜" 비교 column
- R-REPO-6 작동 사례 영구 보존
- Phase 2 archaeology 가치
- 표 형식:
  | State (ground truth) | 사용자 안 (T002.5 Step 1) | 정합 |
  | Draft | (포함 X) | 누락 |
  | Sent | (포함 X) | 누락 |
  | Paid | Paid | 일치 |
  | Archived | (포함 X) | 누락 |
  | Void | (포함 X) | 누락 |
  | (포함 X) | Pending | 가짜 |
  | (포함 X) | Failed | 가짜 |
  | (포함 X) | Refunded | 가짜 |
  | (포함 X) | Disputed | 가짜 |

(나) Stripe webhook ⓑ — invoice 본체 + stripe_status sub-section
- invoices.status state machine = 5 transitions (단순)
- stripe_status sub-section = audit-only 명시
- separate entity 는 과도 (5 entities 유지, #6 추가 안 함)

(다) Booking 9th "Pending" ⓑ — §X.fix incidental sub-section + F7 등록
- 8 main state machine + §X.fix subsection (Pending outlier 분석)
- F7 신규 R-REPO-5 incidental 등록 (T002.5 Step 1 발견)
- Phase 2 normalisation prescription (literal 통일)

═══════════════════════════════════════════════════
[Decision] CF-010 본문 재작성
═══════════════════════════════════════════════════

현재 CF-010: "Stripe webhook 8 누락 transition"

재작성:
- 제목: "Stripe payment lifecycle 와 invoice document lifecycle 분리"
- evidence:
  - invoices.status (5-state document lifecycle: Draft/Sent/Paid/
    Archived/Void)
  - stripe_status (별도 컬럼, payment_failed/refunded audit-only)
  - invoices.status 는 Paid 후 영구 (chargeback / refund 후에도 
    변경 안 됨)
- 영향:
  - 회계 정확성: "Paid" 가 실제 받은 돈 의미하지 않음 (refund 후에도 Paid)
  - 운영: chargeback / dispute 추적 불가능 (stripe_status audit row 
    만 존재)
  - Phase 2: lifecycle 통합 vs 분리 결정 필요
- Phase 2 권장 수정:
  - Option A: invoices.status 확장 (Refunded/Disputed 추가)
  - Option B: stripe_status 별도 entity (Payment events)
  - Option C: invoice.status 변경 trigger 추가 (refund → Refunded)

이건 T002.5 atomic commit 의 일부 (CF-010 본문 갱신).

═══════════════════════════════════════════════════
[T002.5 Step 2 작성 가이드]
═══════════════════════════════════════════════════

[ZZZZ] 5 entities Mermaid stateDiagram-v2
1. Bookings (8 + 1 outlier)
2. Contracts (7)
3. Invoices (5) + Stripe sub-section
4. Work_orders (6)
5. Cs_tickets (3)

각 entity:
- Mermaid stateDiagram-v2
- transition table (trigger / preconditions / side effects)
- audit log 호출 여부 (CF-008 cross-ref)
- gated vs ungated (CF-022 cross-ref)
- (가) ⓑ ground truth + 사용자 안 가짜 비교 column

[AAAAA] CF-010 invoice section ⓑ
- invoices.status 5-state machine (Draft → Sent → Paid → Archived / Void)
- stripe_status sub-section:
  - "audit-only — does not change invoices.status"
  - payment_failed (stripe.ts:77) audit row only
  - refunded (stripe.ts:92) audit row only
  - chargeback/dispute 미처리

[BBBBB] CF-022 cross-pack 종합 표
5 entities × transition 가드 패턴:
- bookings 7/7 (or 9/9 with Pending outlier?) cross-pack leader
- contracts ?/? gated
- invoices ?/? gated
- work_orders ?/? gated (free-transition 검증)
- cs_tickets ?/? gated

[CCCCC] CF-014 contracts multi-step
contracts.activate transition:
- contracts.ts:55-237 generateContractInvoicesAndSchedules
- ≥27 mutations / no-tx
- silent dual-billing 시나리오 명시

[DDDDD] CF-019.a evidence cross-ref
invoices state machine 의 stripe_payment_intent_id 컬럼:
- write site (어느 transition?)
- never read = storage orphan
- Phase 2 cleanup 대상

[EEEEE] CF-023 cross-check
- bookings state machine 의 booking_ref 생성 시점
- leads.ts:175-204 (CF-023.a) 가 어느 transition? = lead 변환 (booking 
  state 외 별도)
- guest-portal.ts:138-141 (CF-023.b) 가 어느 transition? = booking 신청
- public.ts insertLeadWithGeneratedRef = 정상 helper

[FFFFF] §X.fix Booking "Pending" outlier
guest-portal.ts:160 "Pending" literal:
- 8 main state 외 inconsistent literal
- F7 신규 incidental 등록
- Phase 2 normalisation: "Pending" → 8 main state 중 하나로 통일
  - PendingPayment 또는 PendingApproval 매핑?

[GGGGG] Self-check + spot-check
- 5 entities × transition × dimensions
- 3 spot-check claims

═══════════════════════════════════════════════════
[Step 5 atomic commit 범위]
═══════════════════════════════════════════════════

T002.5 통합 atomic commit:

1. state-machines.md (NEW, 600-800 lines)
2. CRITICAL_FINDINGS.md:
   - CF-008 evidence (5 entities audit coverage)
   - CF-010 본문 재작성 (invoice document vs Stripe payment lifecycle)
   - CF-014 evidence (contracts multi-step transition)
   - CF-019.a evidence (invoices stripe_payment_intent_id state)
   - CF-022 cross-pack 종합 표 (5 entities)
   - CF-023 cross-check (booking_ref 생성 위치)
   - F7 신규 incidental (Booking "Pending" outlier)
3. INDEX.md banner T002.5 추가
4. _T002_PROGRESS.md:
   - row 53 (T002.4) commit hash fill
   - row 54 (T002.5) 추가
5. session_plan.md:
   - T002.5 DONE
   - 🎯 T002 COMPLETE marker
   - T003 NEXT marker

═══════════════════════════════════════════════════
[T002.5 사이즈 예측]
═══════════════════════════════════════════════════

5 entities × ~120 lines + cross-pack 종합 + CF-010 재작성 + (가) ⓑ 
비교 column = 600-800 lines
TRIPWIRE EXEMPT (cap 1500 / tripwire 850 모두 미달)

═══════════════════════════════════════════════════
[R-REPO-9 적용 — Step 2~6 자동 진행]
═══════════════════════════════════════════════════

Step 1 결정 사항 모두 사용자 승인 ✅:
- Corrected 5 entities enumeration
- 모두 ⓑ trade-off 채택
- CF-010 본문 재작성
- F7 신규 incidental

차단 조건 미해당 (자동 진행 가능):
- 신규 P0/P1 가설 0
- TRIPWIRE EXEMPT
- R-REPO-6 환각 해결 (corrected 채택)

→ Step 2~6 자동 진행

═══════════════════════════════════════════════════
[지금 시작]
═══════════════════════════════════════════════════

(1) [에이전트] T002.5 Step 2 본문 작성 진입
    - 5 entities Mermaid stateDiagram-v2
    - ZZZZ-GGGGG 가이드 적용
    - TRIPWIRE EXEMPT
(2) Step 3-5 자동 진행
(3) Step 6 통합 보고 (R-REPO-9 적용)
(4) [사용자] git push origin main
(5) 사용자 proceed → 🎯 T002 COMPLETE → T003 (도메인 로직) 진입

T003 자동 시작 절대 금지.
T002 COMPLETE 후 T003 진입 결정 (R-REPO-9 + 묶음 위임 검토).