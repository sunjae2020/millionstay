T002.2.e Step 1 승인. 

7-file ground truth + 사이즈 예측 method-별 정교화 + CF-022 candidate 
사전 식별 + CANDIDATE 상태 유지 + Tripwire 수치 명시 — 모두 적정. 
R-REPO-6/7 자연 통합. 

다만 사용자 git push 누락 인지 → Step 2 진입 전 우선 처리.

═══════════════════════════════════════════════════
[사용자 책임 누락 인정]
═══════════════════════════════════════════════════

Unpushed 27 commits → R-REPO-1 v2 (c) 사용자 책임 미이행:
- T002.2.d 보고 후 push 안 함 (26 누적)
- T002.2.d.fix-1 commit 추가 (27 누적)

Replit Shell 에서 즉시 실행 (사용자 직접):
git push origin main

완료 확인:
git log origin/main..HEAD
# empty 출력 = sync 완료

이후 매 sub-task 완료 시 push 의무.

═══════════════════════════════════════════════════
[Decision] T002.2.e Step 2 진입 — proceed (push 후)
═══════════════════════════════════════════════════

승인 사항:
- 7-file inventory + 51 endpoint ground truth ✅
- 사이즈 예측 ~1055-1105 (보정 모델 적용) ✅
- 3 spot-check 사전 sealing (C1/C2/C3) ✅
- CF-019.a candidate 유지 + T002.3 보류 ✅
- Tripwire 35 ep 시점 840 lines 비교 ✅

═══════════════════════════════════════════════════
[CF-022 candidate 처리 가이드]
═══════════════════════════════════════════════════

C3 에서 사전 식별된 CF-022 후보 (state transition 가드 누락):

작성 중 검증 항목:
- leads.ts PATCH endpoint: state field + transition 가드 점검
- tasks.ts PATCH endpoint: 동일
- cs-tickets.ts PATCH endpoint: 동일
- 가드 패턴:
  - precondition check (현재 상태 검증)
  - role-based authorization (특정 상태는 특정 role 만)
  - audit logging (transition 추적)
- 가드 누락 시 임의 상태 전이 가능 시나리오 enumerate

발견 결과:
(a) leads/tasks/cs-tickets 모두 가드 누락 → CF-022 신규 등재 후보 
    (P1 검토)
(b) 일부만 누락 → CF-018 .c sub-pattern 또는 CF-020 변형
(c) 모두 가드 있음 → C3 negative result, candidate 해제

T002.2.e Step 5 atomic commit 시 결정 + CRITICAL_FINDINGS 갱신.

CF-022 신규 등재 시 P1 검토 근거:
- state transition 무가드 = 권한 우회 + 데이터 무결성 위험
- audit 누락 시 invisible 상태 변경 (CF-008 compound)
- ops-crm 핵심 비즈니스 로직 (CRM workflow) 직결

═══════════════════════════════════════════════════
[추가 가이드 — ops-crm 작성 시]
═══════════════════════════════════════════════════

[V] CF-001 work-orders.cost real anchor
T002.2.e Step 1 식별: work-orders.ts schema cost: real

CF-001 evidence section 에 추가:
- ops-crm.work_orders.cost (real)
- 데이터 흐름: work order 비용 → invoice 청구? expense?
- 작성 중 cross-domain 흐름 확인

[W] CF-008 ops-crm severity matrix row
ops-property 13.3% / ops-catalog 0% / ops-crm 결과:
- 0% 또 → 도메인 severity matrix 갱신
- > 0% → 패턴 변동 (CRM 이 더 audit?)
- inverse correlation 가설 추가 데이터

[X] CF-018/020 ops-crm 적용
- CF-018 nested handler (예: tasks 의 sub-resource)
- CF-020.a GET-leak (deleted_at 필터)
- CF-020.b zombie revival (UPDATE 가드)

[Y] CF-019 도메인 횡단 점검
- ops-crm schema 컬럼 vs route write 비교
- write-orphan 또는 compute drift 발견 시 evidence 누적

═══════════════════════════════════════════════════
[Step 2~6 진행]
═══════════════════════════════════════════════════

(1) [사용자] git push origin main (Replit Shell)
(2) [에이전트] Step 2 본문 작성 (51 endpoint, ops-crm.md 단일)
    - tripwire 35 ep / 840 lines 임계 모니터링
    - 70% mid-write 체크 의무
(3) Step 3 self-check 표 (51 × 7 = 357 cells)
(4) Step 4 spot-check 검증 (C1/C2/C3 + CF-022 candidate 결정)
(5) Step 5 R-REPO-1 v2 atomic stage:
    - ops-crm.md (NEW)
    - CRITICAL_FINDINGS.md (CF expansion + CF-022 결정)
    - INDEX.md (필요 시)
    - _T002_PROGRESS.md (T002.2.e DONE + ledger row 17 fill 
      [9aba01d] + row 18 추가)
    - session_plan.md (T002.2.e DONE, T002.2.f NEXT)
    - cross-ref back-fill (해당 시)
(6) Step 6 R-REPO-4 형식 보고
(7) [사용자] git push origin main (R-REPO-1 v2 (c))

(7) 후 사용자 proceed → T002.2.f (portal-guest.md) 시작.
T002.2.f 자동 시작 절대 금지.

═══════════════════════════════════════════════════
[지금 시작]
═══════════════════════════════════════════════════

사용자 push 병렬 실행 + Step 2 본문 작성 진입.
35 ep 도달 시 lines 측정 + 840 비교 → 보고 1줄 (tripwire status).