The project scaffold is set up. Now build the PROPERTY module completely.

Build in this exact order:

─────────────────────────────────────────
1. SUBURB  (/property/suburbs)
─────────────────────────────────────────
Server:
  - GET/POST/PUT/DELETE /api/v1/suburbs
  - Filter by: country_code, state
  - Search by: name, area_name

Client:
  - List page: columns [Name, Area, State, Country, Status, CreatedOn]
  - Detail/Form page: all fields from schema
    (name, state, postcode, country_code, area_name, lat/lng)

─────────────────────────────────────────
2. PROPERTY  (/property/properties)
─────────────────────────────────────────
Server:
  - GET/POST/PUT/DELETE /api/v1/properties
  - PATCH /api/v1/properties/:id/status (approval workflow)
  - Filter by: approval_status, owner_account_id, suburb_id
  - Include: owner account name, suburb name in list response

Client:
  - List page: columns [Name, Address, Owner, Suburb, Status, CreatedOn]
  - Status badge colors:
      Pending = yellow | Active = green | Suspended = red | Rejected = gray
  - Detail page: all fields + Related tabs [Account, Space]
  - Approval action button (Admin only): Pending → Active

─────────────────────────────────────────
3. SPACE OPTIONS  (/property/space-options)
─────────────────────────────────────────
Server: basic CRUD /api/v1/space-options

Client:
  - List + Detail pages
  - Columns: [Name, Display Name, Category, Status]

─────────────────────────────────────────
4. SPACE POLICY  (/property/space-policies)
─────────────────────────────────────────
Server: basic CRUD /api/v1/space-policies

Client:
  - Detail form with boolean toggles (same_gender, lady_only, 
    no_pet, no_smoking, meal_option) displayed as Yes/No radio buttons
  - minimum_age number input

─────────────────────────────────────────
5. SPACE  (/property/spaces)  ← most complex
─────────────────────────────────────────
Server:
  - GET/POST/PUT/DELETE /api/v1/spaces
  - GET /api/v1/spaces/:id/availability  (30-day calendar)
  - POST /api/v1/spaces/:id/availability/block  (manual block dates)
  - Include in list: property name, parent space name, policy name

Client:
  - List page with advanced filters:
    [Space Type, Status, Property, Booking Mode]
  - Detail page sections:
    General: name, manual_input checkbox
    Property: property lookup field
    Space: parent_space lookup field  
    Main: space_type dropdown, custom_type_name,
          space_option_map (multi-select lookup — replaces Option1~5),
          max_occupancy, booking_mode dropdown
    Policy: space_policy lookup
    Pricing: base_weekly_price, base_currency, min_stay_weeks
    Accounts: landlord_account lookup
    Others: description, floor_number, floor_area_sqm
    OTA Sync: ical_import_url input field
    Owner/Admin section
  - Related tabs: [Contract, Product, ParentSpace, SpaceImage]

─────────────────────────────────────────
SHARED COMPONENT: LookupField
─────────────────────────────────────────
Create a reusable <LookupField> component used across all detail pages:
  Props: { label, value, displayText, onSearch, onClear, required }
  Behavior:
    - Shows selected value in orange (like Edubee)
    - Search icon opens a modal with search input + results list
    - X button clears selection
    - Required validation shows red border if empty on submit

─────────────────────────────────────────
SHARED COMPONENT: StatusBadge
─────────────────────────────────────────
<StatusBadge status="Active|Pending|Suspended|Rejected|...">
  - Color mapping defined centrally
  - Pill shaped (rounded-full, px-2 py-0.5, text-xs)

Write all server routes, controllers, and React pages completely.
No partial code. No TODOs.